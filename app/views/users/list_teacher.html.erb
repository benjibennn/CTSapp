<!-- Two parts to this, one if would show all teachers under admin. one would show students under teacher -->
<h1> Display all teachers </h1>

<table>
  <tr>
    <th>First Name</th>
    <th>Last Name </th>
    <th>Gender </th>
    <th>Phone </th>
  </tr> 

  <% @teacher.each do |teacher| %>

	  <tr>
	    <td><%= teacher.first_name%></td>
	    <td><%= teacher.last_name%></td>
	    <td><%= teacher.gender%></td>
	    <td><%= teacher.phone%></td>
	  </tr>

  <% end %>
</table>

<a href="/users/<%= current_user.id%>/create_teacher"> Create Teacher </a>